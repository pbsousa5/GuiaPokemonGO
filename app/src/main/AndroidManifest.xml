<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.extractor.guiapokemongo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.dicas.DicasActivity" />
        <activity android:name=".view.dicas.DicasBaseActivity" />
        <activity
            android:name=".view.easterEggs.EasterEggsActivity"
            android:label="@string/title_activity_easter_eggs" />
        <activity
            android:name=".view.easterEggs.EasterEggsBaseActivity"
            android:label="@string/title_activity_easter_eggs_base"></activity>
    </application>

</manifest>